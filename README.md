# Browser behaviour

## Chrome

Does not propose installation, but it can be done with button and JavaScript.

## Firefox

Proposes installation on its own after some time, but the JavaScript used with Chrome does not work. Also, when installed manually, the icon is not shown somehow.

